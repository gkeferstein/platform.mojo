{
  "name": "platform.mojo",
  "version": "1.0.0",
  "private": true,
  "description": "MOJO Platform - Dokumentation, Templates, Scripts und Shared Packages",
  "repository": {
    "type": "git",
    "url": "https://github.com/gkeferstein/platform.mojo.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "author": "MOJO Institut",
  "license": "UNLICENSED"
}

