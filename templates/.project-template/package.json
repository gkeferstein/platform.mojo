{
  "name": "{APP_NAME}",
  "version": "1.0.0",
  "description": "{APP_NAME} - MOJO Ökosystem",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "echo \"Tests not configured\" && exit 0",
    "build": "echo \"Build not configured\" && exit 0",
    "lint": "echo \"Lint not configured\" && exit 0"
  },
  "keywords": [
    "mojo",
    "mojo-institut"
  ],
  "author": "MOJO Institut",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}

